/** 
 1. The code prints back for numPlanests with 8 and yearNeptuneDiscovered with 1846.
  
 2. The code will be returning back yearNeptuneDiscovered: 1846 and yearMarsDiscovered: 1659 without numPlanets.

 3. The code that returns from the following code are:
    getUserData({firstName: "Alejandro", favoriteColor: "purple"}) // Your name is Alejandro and you like purple
    getUserData({firstName: "Melissa"}) // your name is Melissa and you like green
    getUserData({}) // your name is undefined and you like green

4. The code returns each string base on its corresponding position. First returns Maya, second returns Marisa, and third returns Chi.

5. raindrops returns "Raindrops on roses", whiskers returns "whiskers on kittens", and ...aFewOfMyFavoriteThings returns ["Bright copper kettles","warm woolen mittens","Brown paper packages tied up with strings"]

6. numbers returns as [10,30,20].

7. const obj = {
    numebers: {
        a: 1,
        b: 2
    }
}
const {a,b} = obj.numbers 

8. [arr[0],arr[1]] = [arr[1], arr[0]]

9. const raceResults = ([first, second, third, ...rest]) => ({first, second, third, rest})
*/